insert into smg_link(name,category_id,link,dept_id,target,priority) select name,dept_cate_id,link,dept_id,target,priority from smg.smg_dept_link

insert into smg_comment(nick_name,comment,ip,resource_type) select commenter,content,ip,"zongcai" from smg.smg_subject_comment

insert into smg_news_debug(dept_id,dept_category_id,title,created_at) select dept_id,dept_category_id,title,created_at from smg_news where dept_category_id!=0

select * from smg_news where title like "%纪实频道获传媒十佳好人好事%"


insert into smg_comment(nick_name,title,comment,ip,resource_type) select commenter,title,content,ip,"yhdjs" from smg.smg_dept_comment