1, 服务器上的域名解析服务一定要配置正确，让服务器上的PHP程序可以正常访问外部主机，并且可以用fopen方式访问http://这种形式的文件URL，否则会引起缩略图功能和采集功能不正常。
   要验证是否正确，可以选择一个图片URL，放进下面这段文本代码，然后保存成testfopen.php上传到服务器上执行，代码如下：
  <?php
     if ( fopen('http://你的图片URL地址') )
        { echo '打开成功'; }
     else
        { echo '打开失败'; }
  ?>

注意：如果你的服务器上的php配置没有打开错误信息显示，那么在打开失败时，还很有可能一片空白，看不到任何显示。这需要打开PHP的错误信息显示才能看到，这可以修改php.ini实现或联系空间商。


----------------------------

2，在结点名称里英文符号只能使用下划线和减号还有圆括号，请不要用 &/`\?'";<> 这些英文半角符号，否则会引起结点树显示不正常，甚至引起调用标签显示内容不正常。

----------------------------

3, 在专业版中，投稿工作流程不能自定义，只能选择系统默认的流程，所以，要求系统中具备编辑和高级编辑这两个用户组，而且，需要是3和4的id号，在安装时是有的，请不要删除。